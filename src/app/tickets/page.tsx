import { TicketFlow } from "@/components/ticket-flow";

export const metadata = {
  title: "Buy Tickets | Bitzsznn",
  description: "Choose your Freed At Last ticket tier and reserve your spot.",
};

export default function TicketsPage() {
  return <TicketFlow />;
}
